---------- Useful information for arch linux ----------

[GRUB]
startx .xinitrc

[USB]
lsblk
sudo lbkid
sudo mount /dev/sdb1/ ~/usb/

[DMENU]
dmenu_run -nb '#000000' -nf '#FFFFFF' -sb '#FFFFFF' -sf '#000000'

[FEH]
feh --bg-fill ~/pictures --randomize

[PACKAGES]
gimp
feh
pipewire, pipewire-pulse, pipewire-media-session // ? pulseaudio
ranger, thunar
alacritty
bspwm, sxhkd, polybar, xorg, picom, nvidia, nvidia-utils, gtk
// ? bash completion package

[AUR]
https://aur.archlinux.org/upscayl-bin
https://aur.archlinux.org/librewolf-bin
https://aur.archlinux.org/osu // not working (need wine?)

[LIBREWOLF]
extensions: Dark Reader, uBlock Origin
theme: Black

[GITHUB]
git clone https://github.com/<user_name>/<repo>
git clone https://<token>@github.com/<user_name>/<repo>
// ? git add <file>
// ? git del <file>
// ? git commit -m ""
// ? git checkout <branch>
// ? git clone -b <branch> https://github.com/<user_name>/<repo>
// ? git push origin main (<source> <branch>)

[CMD]
xset r rate 200 30
setxkbmap se
killall
pkill
shutdown 0
reboot
SUPER + ENTER (terminal)
SUPER + SPACE (dmenu)

[TORRENT]
rtorrent <torrent>

[OTHER]
manga: https://comick.cc
anime: https://anix.to

https://www.bugswriter.com/
